/**
 * Word Definition Popup Component
 *
 * Used to display detailed word definitions when users click on words.
 * This is a React component reimplementation of the original class-based version.
 */

// Export the WordPopupManager class as WordPopup for backward compatibility
export { WordPopupManager as WordPopup } from './WordPopupManager';

// Also export the React component directly for those who want to use it
export { WordPopup as WordPopupComponent } from './WordPopup'; 